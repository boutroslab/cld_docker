version: "3"
services:
    talencrisp:
        image: cld_docker_new:latest 
        volumes:
            - ~/Desktop:/data
        environment:
            - LANG=C
            - DISPLAY=$IP:0
            - RSYNC_PROXY=""
            - http_proxy=""
        command: cld_gui